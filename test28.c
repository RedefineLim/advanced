#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main()
{
    // char arr[] = "abcdef";
    char arr[] = {'a', 'b', 'c', 'd', 'e', 'f'};
    int len = strlen(arr);

    printf("%d \n", len);
    // printf("%s \n", arr);

    system("pause");
    return 0;
}